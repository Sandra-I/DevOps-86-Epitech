server {
    listen 80;
    listen [::]:80;

    root /var/www/front.testlab.com/front/dist/front;
    index index.html index.htm index.nginx-debian.html;

    server_name www.front.testlab.com;

    location / {
        try_files $uri $uri/ =404;
    }
}